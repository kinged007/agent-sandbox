# Copy to .env — never commit .env
NINEROUTER_KEY=sk-...
PI_PROVIDER=9router
# allow pi/opencode to see these through Bernstein's filtered env (survives upgrades, unlike site-packages patches)
BERNSTEIN_ALLOWED_API_KEY_ENVS=NINEROUTER_KEY,PI_PROVIDER
